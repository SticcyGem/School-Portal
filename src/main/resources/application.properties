spring.application.name=school-portal
# app.cors.allowed-origins=http://emmanuel-laptop.local:4321
jwt.secret=5c88aa9080bab9e5a4ab28f0707f0176c8d30f1ede8ac4445852d97add6fe18f
jwt.expiration=3600000

# --- Database Connection (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/schoolportal
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate Settings ---
# Explicitly tell Hibernate to speak PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# --- Spring Session Configuration ---
# CHANGED: Set to 'always' so Spring creates the session tables (SPRING_SESSION, etc.)
# automatically in your new empty Postgres database.
spring.session.jdbc.initialize-schema=always